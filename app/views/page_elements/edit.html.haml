= form_for @page_element, :url => landing_page_page_element_path(@landing_page, @page_element) do |f|
  = f.label :label
  = f.text_field :label
  = f.label :value
  = f.text_field :value
  = f.submit